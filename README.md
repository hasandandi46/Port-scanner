# Python Port Scanner

A simple port scanner built in Python that identifies open ports on a target system using socket programming.

## Features
- Scans common TCP ports
- Identifies open and closed ports
- User input for target host

## How to Run
1. Clone the repository
2. Run: python main.py
3. Enter a target (e.g., scanme.nmap.org)
